# Copy to ~/.config/arda/varda/external-lane.env before enabling
# The Varda HTTP service must expose POST /external_lane/import.
ARDA_VARDA_URL=http://127.0.0.1:5111
# Crawl4AI service base URL; the importer appends /md.
ARDA_CRAWL4AI_URL=http://127.0.0.1:11235
ARDA_EXTERNAL_LANE_FILTER=fit
