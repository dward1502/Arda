# sigil: SCROLL
# Arda canonical environment template. Copy to local `.env` and replace placeholders.
# Source of truth for profile paths/kinds: `config/templates/arda.local.profile.toml`

ARDA_ROOT=${HOME}/Eregion/Arda
ARDA_CONFIG_DIR=${ARDA_ROOT}/config
ARDA_DATA_DIR=${ARDA_ROOT}/data
ARDA_CACHE_DIR=${XDG_CACHE_HOME:-${HOME}/.cache}/arda
ARDA_RUNTIME_DIR=${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/arda

ARDA_TASK_QUEUE_PATH=core/projects/tasks/queue.jsonl
ARDA_PROJECT_TASK_QUEUE_PATH=core/projects/tasks/queue.jsonl
ARDA_WARDEN_QUEUE_PATH=data/warden/informant_queue.jsonl

ARDA_MANWE_HOME=${HOME}/.manwe
ARDA_HADES_HOME=${HOME}/.hades
ARDA_HERMES_HOME=${HOME}/.hermes
ARDA_ATHENA_HOME=${HOME}/.athena
ARDA_PROMETHEUS_HOME=${HOME}/.prometheus
ARDA_MNEMOSYNE_HOME=${HOME}/.mnemosyne

ARDA_ATHENA_SOCKET=${ARDA_RUNTIME_DIR}/athena.sock
ARDA_MANWE_SOCKET=${ARDA_RUNTIME_DIR}/manwe.sock
ARDA_HADES_SOCKET=${ARDA_RUNTIME_DIR}/hades.sock
ARDA_HERMES_SOCKET=${ARDA_RUNTIME_DIR}/hermes.sock
ARDA_PROMETHEUS_SOCKET=${ARDA_RUNTIME_DIR}/prometheus.sock
ARDA_MNEMOSYNE_SOCKET=${ARDA_RUNTIME_DIR}/mnemosyne.sock

OPENCODE_API_KEY=
OPENROUTER_API_KEY=
OPENAI_API_KEY=
