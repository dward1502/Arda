# sigil: SCROLL
# Arda canonical environment template. Copy to local `.env` and replace placeholders.
# Source of truth for profile paths/kinds: `config/templates/arda.local.profile.toml`

ANNUNIMAS_ROOT=${HOME}/Annunimas
ANNUNIMAS_CONFIG_DIR=${XDG_CONFIG_HOME:-${HOME}/.config}/annunimas
ANNUNIMAS_DATA_DIR=${XDG_DATA_HOME:-${HOME}/.local/share}/annunimas
ANNUNIMAS_CACHE_DIR=${XDG_CACHE_HOME:-${HOME}/.cache}/annunimas
ANNUNIMAS_RUNTIME_DIR=${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/annunimas

ANNUNIMAS_TASK_QUEUE_PATH=core/projects/tasks/queue.jsonl
ANNUNIMAS_PROJECT_TASK_QUEUE_PATH=core/projects/tasks/queue.jsonl
ANNUNIMAS_WARDEN_QUEUE_PATH=data/warden/informant_queue.jsonl

ANNUNIMAS_CHARON_HOME=${HOME}/.charon
ANNUNIMAS_HADES_HOME=${HOME}/.hades
ANNUNIMAS_HERMES_HOME=${HOME}/.hermes
ANNUNIMAS_ATHENA_HOME=${HOME}/.athena
ANNUNIMAS_PROMETHEUS_HOME=${HOME}/.prometheus
ANNUNIMAS_MNEMOSYNE_HOME=${HOME}/.mnemosyne

ANNUNIMAS_ATHENA_SOCKET=${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/annunimas/athena.sock
ANNUNIMAS_CHARON_SOCKET=${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/annunimas/charon.sock
ANNUNIMAS_HADES_SOCKET=${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/annunimas/hades.sock
ANNUNIMAS_HERMES_SOCKET=${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/annunimas/hermes.sock
ANNUNIMAS_PROMETHEUS_SOCKET=${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/annunimas/prometheus.sock
ANNUNIMAS_MNEMOSYNE_SOCKET=${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/annunimas/mnemosyne.sock

OPENCODE_API_KEY=
OPENROUTER_API_KEY=
OPENAI_API_KEY=
