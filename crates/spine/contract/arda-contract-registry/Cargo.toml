[package]
name = "arda-contract-registry"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
glob-match = "0.2"

[dev-dependencies]
tempfile = "3"
walkdir = "2"
