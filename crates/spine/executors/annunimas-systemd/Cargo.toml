# sigil: REPAIR
[package]
name = "annunimas-systemd"
version = "0.1.0"
edition = "2021"
description = "Thin typed client for systemctl --user, consumed by Annunimas service-health monitors."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
tempfile = "3"
