# sigil: REPAIR
[package]
name = "arda-ceo"
version = "0.1.0"
edition = "2021"

[dependencies]
arda-prometheus = { path = "../arda-prometheus" }

[dev-dependencies]
tokio = { version = "1.0", features = ["full"] }
async-trait = "0.1"
tempdir = "0.3"
arda-core = { path = "../../spine/governance/arda-core" }
serde_json = "1.0"
