# Interop landscape (deferred)

- Known compatibility surfaces: `loop_engine`, `governance_gates`, `background`.
- No live `GEN2/GEN3` artifacts exist in `arda-core` at this revision.
- Defer interop-specific coverage until a consumer crate introduces the shared scenario.
- Preserve existing unit tests and truncation-aware evidence in package docs.
