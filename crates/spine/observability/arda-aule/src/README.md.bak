---
soterion:
  sigil: "ANKH"
  glyph: "🜃"
  code_point: "U+1F728"
  role: "organization_index"
  owner: "ARDA"
  status: "active"
  last_reviewed: "2026-07-14"
---

> 🜃 Arda-Council: 📜 organization_index | owner: ARDA | status: active | reviewed: 2026-07-14

# src

Source root for `crates/spine/governance/arda-aule`.

## Contents

- `lib.rs` — module exports (`contract`, `council`, `service`), `crate_identity()`, doctests.
- `contract.rs` — `ArdaCouncilContract` + `GovernanceBaseline` / `ContinuityBaseline` (the sovereign-agent blueprint).
- `council.rs` — `CouncilSeat`, `QueryMode`, `CouncilQuery`, `CouncilBrief`.
- `service.rs` — `status()`, `build_brief()`.

See `INDEX.md` for the deterministic child listing and the surface-API table.
