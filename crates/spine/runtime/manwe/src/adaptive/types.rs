//! Adaptive routing uses the canonical Manwe domain types.
//!
//! Type definitions live at the crate root so the stable gateway, adaptive
//! router, transports, and tests cannot drift into incompatible Rust types.

pub use crate::types::*;
