# arda-varda archive

Historical evidence retained outside the crate source tree.

- `design-assessment-2026-06-09.md`: superseded design assessment. Current implementation truth lives in `crates/spine/executors/arda-varda/{README.md,BREAKDOWN.md,PLAN.md,STATUS.md}`.
- `bacon-lite-validation-log-legacy.md`: legacy generated Bacon-Lite validation log moved from the crate-local `docs/` tree. The live canonical governance log remains `docs/operator/library/governance/bacon_lite.md`.

These files are evidence, not active plans. The crate must not recreate a nested `docs/` directory.
