# P5.1 live Personal Operations acceptance

Date: 2026-08-10
Maturity: `workflow_proven`, `personal_continuity_verified`, `restart_recovery_verified`
Remaining maturity: `operator_accepted` and the P5.5 one-week usefulness/burden review

## Scope

This receipt retains the live evidence for the short P5.1 acceptance gates. It does not claim that the one-week dogfood window has elapsed, that the operator accepted reminder burden, or that optional wellness support is approved.

## Actual operator-path capture and restart

The authenticated Discord capture already retained by the P2.4 acceptance entered the canonical operator-session ledger at `2026-08-09T09:00:04.172000Z` and the Personal Operations event log at `2026-08-09T09:00:06.064602443Z`. The receive-to-durable interval was 1.892602 seconds, below the five-second gate. Its canonical capture ID is `4648e678-9534-5c2d-bb5b-6fdd44884cd7`.

After deployment and a second explicit `arda.service` restart on 2026-08-10, the authenticated operator projection still contained exactly that capture. Deleting the isolated acceptance operator's records did not remove it.

## Live correction, brief, resume, and reminder proof

An isolated loopback-only acceptance operator exercised the deployed release binary through the canonical HTTP surface:

1. Durable capture returned HTTP 201 in 13.831 ms.
2. An inferred `task` classification appeared in the sourced morning brief.
3. An appended operator-authored correction changed the same projected item to `reminder`; the morning brief cited all five personal source events.
4. A delivered reminder was deferred once. Replaying the same defer returned HTTP 409 (`reminder has no unacknowledged delivered attempt`) and appended no duplicate response.
5. After restart, the item remained `reminder`, the delivery state remained `deferred`, and the resume projection reported one unique active item rather than double-counting the same item in `today` and `waiting`.

The live run exposed and closed three bounded gaps:

- operator-authored reclassification could not correct an earlier operator-authored classification;
- Personal Operations reads did not isolate operators once more than one operator had records,
  and item mutations did not reject a different authenticated operator;
- resume active counts double-counted items present in both `today` and `waiting`.

## Export, deletion, and privacy boundary

Before cleanup, authenticated export returned the isolated operator's five events. Idempotent deletion removed those five events and replay returned the same deletion receipt. The deletion receipt is `b412bf95-1950-5d6f-b481-b235fbe4f898`.

The `data/runs/` tree contained 10 files before and after deletion and retained the same aggregate SHA-256 digest:

`b6bb4dbe95437fa11e7e0c76c98c6a879c79c6f8e5ffb7e573e33f9749f8ffc1`

The live personal directory and event log modes were `0700` and `0600`. Multi-operator reads now require `x-arda-operator-id` and return only that operator's events. Classification, scheduling, completion, and reminder-attempt mutations reject items owned by another operator. The HUD and authenticated gateway context path send the same identity header. The loopback-only single-operator compatibility path remains explicit and is not remote or multi-user authentication.

## Verification

- `cargo test -p arda-core --test personal_ops --test personal_ops_projection`: 13 passed.
- `cargo test -p arda-engine --test personal_ops_store --test harness_personal_ops --test proactive_cycle -- --test-threads=1`: 21 passed.
- `cargo test -p arda-engine --test harness_operator_messages`: 8 passed.
- `cargo clippy -p arda-core --tests -- -D warnings`: passed.
- Focused engine Clippy passed with only the documented unrelated `too_many_arguments` and `unnecessary_to_owned` allowances.
- `pnpm --dir apps/arda-hud run build`: passed.
- Deployed and source `arda` release binaries matched SHA-256 `41e650bf72f9b1f1905f10e1dfda6e5d36bff1bd8f7d22914f29cb1f72a4d68a`.
- `arda.service` recovered active and `/health` returned `ok`.

## Remaining gate

P5.5 runs from 2026-08-10 through 2026-08-17 in the private operator profile. Its closeout must record usefulness, corrections, interruption timing, duplicates, unsupported inferences, attention burden, privacy/audience errors, and operator disposition. No agent may supply that disposition on the operator's behalf.
