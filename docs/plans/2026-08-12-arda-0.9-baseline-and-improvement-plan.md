---
soterion:
  sigil: "SCROLL"
  glyph: "📜"
  code_point: "U+1F4DC"
  role: "implementation_plan"
  owner: "HADES"
  status: "active"
  last_reviewed: "2026-08-14"
---

> 🜏 Soterion: 📜 implementation_plan | owner: HADES | status: active | reviewed: 2026-08-14

# Arda 0.9 Baseline and Improvement Plan

**Release authority:** [`Arda 0.9 Baseline`](../releases/0.9/BASELINE.md)<br>
**Profile:** personal/internal, single-operator, loopback-only<br>
**Version:** `0.9.0`

## Purpose

Keep one finite active plan after the 0.9 baseline. Work enters this plan only
when it fixes a defect, closes a named evidence gap, improves a supported
workflow measurably, or satisfies an explicit operator need. It may not create a
parallel queue, identity, policy, memory, receipt, runtime, or product authority.

## Baseline closeout

- [x] Reclassify completed release-engineering and convergence plans as historical records.
- [x] Defer the broader 1.0 ecosystem and final-release qualification plans without claiming completion.
- [x] Publish one whole-Arda capability, limitation, evidence, and release-posture baseline.
- [x] Adopt `0.9.0` as the personal/internal maturity checkpoint.
- [x] Record independent flow review as intentionally omitted—not performed—for 0.9.

## Active 0.9 improvement frontier

- [ ] Complete the genuine Personal Operations usefulness/burden disposition after the operator review period ending 2026-08-17; preserve an honest open status if more use is needed.
- [ ] Triage tester/operator defects from the exact 0.9 artifact and fix release-blocking regressions before rebuilding it.
- [x] Assess and resolve or document the HUD's remaining low/moderate production dependency findings.
- [x] Repair the repository-wide stale documentation links that are owned by generated state inventory or vendored historical material without rewriting valid historical evidence.
- [x] Record a measured native HUD/runtime performance and accessibility baseline when the operator chooses final display refinement.

### Improvement evidence

- 2026-08-13 — HUD production dependency findings resolved by upgrading
  `mermaid` from `11.16.0` to patched `11.16.1`, which also advanced
  `dompurify` from `3.4.12` to patched `3.4.13`. `pnpm audit --prod --json`
  reports 0 low, moderate, high, or critical findings across 201 production
  dependencies. `pnpm test -- mermaidCanvasRenderer` passed the configured full
  suite (131 files, 518 tests), `pnpm lint` exited zero with the existing warning
  backlog, and `pnpm build` passed.
- 2026-08-14 — Repository-wide HADES validation checked 423 local Markdown
  links with 0 broken links and 0 active-plan completion-language issues. The
  repair removed 34 deleted entries from the generated `core/state` inventory,
  repaired two stale `core/knowledge/clients` paths, and taught the validator to
  preserve vendored Rust intra-doc links; its focused suite passed 5/5.
- 2026-08-14 — A current-source native HUD baseline was recorded in
  [`hud-native-performance-accessibility-baseline-20260814.json`](../evidence/0.9/hud-native-performance-accessibility-baseline-20260814.json).
  The source-built binary passed compilation and all 518 frontend tests, but the
  hardware-backed X11 path rendered a black webview after a GBM allocation
  failure. The DMABUF-disabled rendering fallback peaked at 647.17 MiB aggregate
  RSS and 106.05% of one CPU core during a 60-second sample, and native WebKit
  exposed no HUD content semantics. This closes baseline recording, not native
  qualification; the defect-triage/rebuild item remains open and fail-closed.

## Explicitly deferred from 0.9

These are not active 0.9 tasks or blockers:

- independent flow/evaluator review;
- independent final release-critical security/code review;
- final signed-byte `1.0.0` qualification;
- phone/desktop shared-state release support;
- payments/Web3, extra devices, Mirromere, RELIC/CITADEL expansion, and broader Company Operations;
- broad local-inference/council or external-product expansion.

They may return only through an explicit product/release decision. The retained
future authorities are under `docs/archive/deferred/1.0/`.

## 0.9 acceptance rule

The baseline remains valid while its supported profile and limitations are
truthful. A source or artifact change requires focused tests and a new checksum;
it does not automatically require independent review. No 0.9 document or UI may
claim independent qualification, public-production readiness, remote safety, or
final `1.0.0` maturity.
